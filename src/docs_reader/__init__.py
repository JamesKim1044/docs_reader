"""docs_reader — LLM-based structured extraction from documents into schema-validated JSON.

Provider-neutral (open-source first, Gemini/Claude as optional escalation) and
quality-first: extraction runs as a multi-stage, multi-pass pipeline rather than a
single LLM call.
"""

__version__ = "0.1.0"
